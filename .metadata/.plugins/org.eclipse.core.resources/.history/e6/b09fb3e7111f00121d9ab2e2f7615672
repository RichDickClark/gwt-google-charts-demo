package edu;

import java.util.List;

import edu.mit.ll.irbenchmark.BenchmarkTask;
import edu.mit.ll.irbenchmark.Benchmarker;
import edu.mit.ll.irbenchmark.IRSystemName;
import edu.mit.ll.irbenchmark.IRSystemRun;

public class scratch {

	public static void main( String[] args ) throws Exception {
		// Prepare an information retrieval system on our test corpus
//		IRSystemName.STEMMING_BM25_LUCENE40.create().buildIndex( CorpusName.ISDSCONUSCORPUS );
		
		// Evaluate IRsystem on our benchmark IR tasks
//		for( IRSystemName irsys : IRSystemName.values() ) {
//			System.out.println( irsys );
//			Benchmarker.benchmarkSystem(irsys);
//		}
		
		List<IRSystemRun> runRes = IRSystemRun.find( IRSystemName.STANDARD_LUCENE36, BenchmarkTask.DISASTER_LONGKEYWORD );
		System.out.println( runRes.get(0).getId() );
	}
	
}
